using UnityEngine;

namespace Mobiversite.GameLib.DevLib.Scene
{
    public class SceneManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
